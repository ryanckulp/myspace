<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapse" data-toggle="collapse" data-target="#bs-example-navbar-collapse">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <h1><%= link_to root_path do %>Omni<% end %></h1>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li>Signed in as: <%= current_user.name %> - <%= image_tag(@auth['info']['image'], width: 50, height: 50) %></li>
          <li><%= link_to "Sign Out", 'sign_out', method: :delete %></li>
        <% else %>
          <li><% link_to "Facebook" "auth/facebook" %> </li>
        <% end %>
      </ul>

    </div>
  </div>  
</nav>